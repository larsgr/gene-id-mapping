name: idmap
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - bedtools
  - agat
  - gffcompare
  - gffread
  - jupyterlab
  - ipykernel
  - pandas
  - matplotlib
  - seaborn
  - nbformat
  # ParsEval (AEGeAn) is not available on osx-arm64 via conda; use Docker instead.
